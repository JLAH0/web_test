// let start = prompt('Beer Number')
const userNumber =  10;

const beerBottles = (userNumber) => {
    const bottlestothewall = 1;
    const bottelToRemove = userNumber -1;
    const textMessage = `start with ${bottelNumber}\n`
    while (bottelRemove >1) {
        textMessage = `Take _${bottlestothewall}_ down, pass it around`;
        textMessage = `we have now ${bottelRemove} bottles`;
        bottlestothewall++;
        bottelRemove--;
    }
    if (bottelRemove===1) {
        textMessage = `Take _${bottlestothewall}_ down, pass it around`;
        textMessage = `we have now ${bottelRemove} bottles`;
        bottlestothewall++;
        bottelRemove--;
    }
}

beerBottles(userNumber)

// while (i < 10) {
//     text += "The number is " + i;
//     i++;
//   }
// while(numbers.length < 5 && i < numbers.length)
// {
//     if(numbers[i] > bigone)
//         bigone = numbers[i];

//     i++;
// }